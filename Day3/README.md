# Day 3 — Bayesian Model Averaging (Python)

This folder contains materials for Day 3 of the Applied Bayesian Methods course.  This README is a template.  If you want to use your own repo then just put the link in the repo.  This is just a suggestion generated by AI.

## Topics

- Bayesian model averaging (BMA)
- Python-based Bayesian workflow

## Folder structure

Add your slides, scripts, and data here. Suggested organisation:

```
day3/
├── slides/
├── data/
└── scripts/
```

## Notes

- Please update this README with your preferred slide format and any package dependencies (e.g. `pymc`, `arviz`, `bambi`) once known.
- A `requirements.txt` or `environment.yml` for the Python environment would be helpful to add here.
- Rendered output (e.g. `*.html`, `*_files/`) is gitignored at the repo level — do not force-add these.
